const fs = require('fs'); const code = fs.readFileSync('server/index.js', 'utf8'); let openBraces = 0, openParens = 0, openBrackets = 0; for(let i=0; i<code.length; i++) { const c=code[i]; if(c==='{') openBraces++; else if(c==='}') openBraces--; else if(c==='(') openParens++; else if(c===')') openParens--; else if(c==='[') openBrackets++; else if(c===']') openBrackets--; } console.log({openBraces, openParens, openBrackets});
