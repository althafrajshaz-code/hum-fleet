const fs = require('fs'); const file = 'data_store.json'; const data = JSON.parse(fs.readFileSync(file, 'utf8')); if (data.activeRides) { data.activeRides.forEach(ride => { if (typeof ride.pickup === 'object' && ride.pickup !== null) { ride.pickup = ride.pickup.address; } if (typeof ride.dropoff === 'object' && ride.dropoff !== null) { ride.dropoff = ride.dropoff.address; } }); } fs.writeFileSync(file, JSON.stringify(data, null, 4)); console.log('Fixed data_store.json');
