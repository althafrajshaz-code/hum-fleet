const puppeteer = require('puppeteer'); (async () => { try { const browser = await puppeteer.launch({headless: 'new'}); const page = await browser.newPage(); page.on('pageerror', e => console.error('PAGE_ERROR_FOUND:', e)); page.on('console', m => console.log('LOG:', m.text())); await page.goto('https://humfleet.com/driver/dashboard'); await new Promise(r => setTimeout(r, 4000)); console.log('Done waiting.'); await browser.close(); } catch(e) { console.error('Script Error:', e); } })();
