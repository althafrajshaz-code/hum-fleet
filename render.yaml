services:
  - type: web
    name: hum-fleet-api
    runtime: node
    region: singapore
    plan: free
    rootDir: server
    buildCommand: npm install
    startCommand: node index.js
    healthCheckPath: /
    envVars:
      - key: MONGODB_URI
        value: placeholder
      - key: NODE_ENV
        value: production
