const puppeteer = require('puppeteer'); (async () => { try { const browser = await puppeteer.launch({headless: 'new'}); const page = await browser.newPage(); await page.setViewport({width: 375, height: 812}); await page.goto('https://humfleet.xyz/driver/login'); await page.evaluate(() => { localStorage.setItem('driverEmail', 'anoop.nair@gmail.com'); localStorage.setItem('driverToken', 'fake'); }); await page.goto('https://humfleet.xyz/driver', {waitUntil: 'networkidle0'}); await new Promise(r => setTimeout(r, 2000)); await page.screenshot({path: 'd:\\\\Althaf\\\\hum\\\\screenshot.png'}); console.log('Screenshot saved.'); await browser.close(); } catch(e) { console.error('Script Error:', e); } })();
